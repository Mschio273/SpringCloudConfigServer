#spring:
#  profiles:
#    active: hml
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/Mschio273/SpringCloudConfig

spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/Mschio/spring-config.git
          clone-on-start: true
        bootstrap: true
      enabled: true

  application:
    name: Config-Server

logging:
  level: debug


#  profiles:
#    active: inactive

server:
  port: 8888
